# Provider keys for the tiers in research.yaml. Default template uses OpenAI only.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Jev (TypeSafe AI) — used for routing, gating, dedupe, stop decisions
TYPESAFE_API_KEY=
